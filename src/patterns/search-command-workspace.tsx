import * as React from "react";
import { cn } from "../lib/cn";
export interface SearchCommandWorkspaceProps extends React.HTMLAttributes<HTMLDivElement> { query: React.ReactNode; filters?: React.ReactNode; resultContent: React.ReactNode; preview?: React.ReactNode; footer?: React.ReactNode; }
export function SearchCommandWorkspace({ query, filters, resultContent, preview, footer, className, ...props }: SearchCommandWorkspaceProps) { return <div className={cn("p-pattern-search-command", className)} {...props}><header className="p-pattern-search-command-header">{query}{filters}</header><main className="p-pattern-search-command-results">{resultContent}</main>{preview ? <aside className="p-pattern-search-command-preview">{preview}</aside> : null}{footer ? <footer className="p-pattern-search-command-footer">{footer}</footer> : null}</div>; }
